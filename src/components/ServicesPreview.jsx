import {ArrowUpRight} from "lucide-react";import {motion} from "motion/react";import Reveal from "./Reveal";import {useContent} from "../context/ContentContext";
export default function ServicesPreview(){const {content}=useContent();return <section className="section"><div className="container"><Reveal><span className="eyebrow">كيف نبدأ</span><h2 className="title-lg">من فكرتك إلى مناسبتك.</h2></Reveal><div className="services-list">{content.services.map((s,index)=><motion.div className="service-row" key={s.id} initial={{opacity:0,x:-18}} whileInView={{opacity:1,x:0}} viewport={{once:true,amount:.4}} transition={{duration:.55,delay:index*.05}}><span className="num">{s.number}</span><h3>{s.title}</h3><p>{s.description}</p><ArrowUpRight size={19}/></motion.div>)}</div></div></section>}
