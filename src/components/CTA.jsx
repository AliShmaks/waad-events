import {ArrowLeft} from "lucide-react";import Reveal from "./Reveal";import {useContent} from "../context/ContentContext";
export default function CTA(){const {content}=useContent();const wa=`https://wa.me/${content.site.phoneRaw}?text=${encodeURIComponent(content.site.whatsappMessage)}`;return <section className="cta-section"><div className="container cta-inner"><Reveal><span className="eyebrow">{content.cta.eyebrow}</span><h2 className="title-xl">{content.cta.title}</h2><a className="btn btn-light" href={wa} target="_blank">{content.cta.button} <ArrowLeft size={18}/></a></Reveal></div></section>}
